{
  "name": "arnold-emergency",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "preview": "astro preview",
    "astro": "astro",
    "generate:classroom": "node scripts/generate-classroom.mjs",
    "generate:terminology": "node scripts/generate-terminology.mjs",
    "build": "pnpm run generate:terminology && astro build",
    "deploy": "pnpm build && npx wrangler pages deploy dist --project-name=arnold-emergency"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.10",
    "astro": "^7.1.6",
    "sharp": "^0.35.3",
    "unist-util-visit-parents": "^6.0.2"
  }
}